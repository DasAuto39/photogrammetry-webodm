services:
  frontend:
    image: <your_dockerhub_username>/persepsi-frontend:latest
    container_name: pr-frontend
    ports:
      - "80:80"
    environment:
      - BACKEND_URL=http://192.168.1.15:5000
    restart: unless-stopped
